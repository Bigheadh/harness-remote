import { describe, it, expect } from "vitest";

// Placeholder: task store tests will be implemented in a future iteration.
describe("task store", () => {
  it.todo("createTask creates a new task");
  it.todo("listTasks filters by status");
});
