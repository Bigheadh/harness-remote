import { describe, it, expect } from "vitest";

// Placeholder: Feishu event tests will be implemented in a future iteration.
describe("feishu events", () => {
  it.todo("parseMessageEvent extracts userId, chatId, text");
  it.todo("duplicate events are deduplicated");
});
